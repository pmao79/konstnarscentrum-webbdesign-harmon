import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminProductGroups() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Produktgrupper</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Produktgrupper admin sida kommer snart...</p>
      </CardContent>
    </Card>
  );
} 