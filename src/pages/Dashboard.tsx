import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function Dashboard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Översikt</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Översiktssida kommer snart...</p>
      </CardContent>
    </Card>
  );
} 