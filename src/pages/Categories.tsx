import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function Categories() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Kategorier</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Kategorisida kommer snart...</p>
      </CardContent>
    </Card>
  );
} 