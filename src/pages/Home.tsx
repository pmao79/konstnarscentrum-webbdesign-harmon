import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function Home() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Välkommen till Konstnärscentrum</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Hemsida kommer snart...</p>
      </CardContent>
    </Card>
  );
} 